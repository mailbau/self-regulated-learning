# URL of the Flask backend API. Not needed at all in demo mode.
NEXT_PUBLIC_API_URL=http://localhost:5000

# Set to "true" to run the app with zero backend, using localStorage-backed mock data.
# See README.md > "Try it with zero setup" for details.
NEXT_PUBLIC_DEMO_MODE=false
